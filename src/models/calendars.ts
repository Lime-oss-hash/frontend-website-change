export interface Calendars {
    _id: string,
    date: string,
    title: string,
    description: string,
    location: string,
    startTime: string,
    endTime: string,
    createdAt: string,
    updatedAt: string,
}